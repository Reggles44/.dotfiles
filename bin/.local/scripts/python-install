#!/usr/bin/env zsh

sudo apt -y install python3 python3-pip python3-setuptools
python -m pip install -U pip || python -m ensurepip || 	sudo apt -y install python3.11-venv
curl https://pyenv.run | bash
python -m pip install pipenv

